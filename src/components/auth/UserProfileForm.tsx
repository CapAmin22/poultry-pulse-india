
import React from 'react';
import ProfileForm from './ProfileForm';

// This component now just serves as a wrapper around the refactored components
const UserProfileForm: React.FC = () => {
  return <ProfileForm />;
};

export default UserProfileForm;
